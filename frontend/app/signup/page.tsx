import { SignupScreen } from "@/components/SignupScreen";

export const metadata = {
  title: "Sign Up - FitTrack",
  description: "Create a new FitTrack account",
};

export default function SignupPage() {
  return <SignupScreen />;
}
